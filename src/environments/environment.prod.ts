export const environment = {
  production: true,
  baseUrl: "https://bergisch-gladbach-grafana.schweren.dev", //I did not want to hardcode the URL here, but loading from environment variables has been removed from Angular because of a CVE
};
